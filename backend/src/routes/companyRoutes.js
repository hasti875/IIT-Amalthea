const express = require('express');
const router = express.Router();

// Placeholder routes - will be implemented as needed
router.get('/', (req, res) => {
  res.json({ message: 'Company routes not implemented yet' });
});

router.get('/', (req, res) => {
  res.json({ message: 'Approval routes not implemented yet' });
});

module.exports = router;